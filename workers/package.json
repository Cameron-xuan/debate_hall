{
  "name": "debate-hall-workers",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute debate-hall --local --file=schema.sql",
    "db:init:remote": "wrangler d1 execute debate-hall --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.2",
    "wrangler": "^4.90.1"
  }
}
